# Secrets template file
# This file should be encrypted with SOPS before committing
#
# To encrypt:
#   sops --encrypt --in-place secrets/secrets.yaml
#
# To edit:
#   sops secrets/secrets.yaml
#
# To decrypt (view only):
#   sops --decrypt secrets/secrets.yaml

# Example secrets structure (replace with your actual secrets)
# These will be available at /run/secrets/<key> when deployed

# API keys and tokens
# api_keys:
#   github_token: "ghp_xxxxxxxxxxxxxxxxxxxx"
#   openai_api_key: "sk-xxxxxxxxxxxxxxxxxxxx"

# Database credentials
# database:
#   postgres_password: "your-secure-password"

# SSH private keys (for services that need them)
# ssh_keys:
#   deploy_key: |
#     -----BEGIN OPENSSH PRIVATE KEY-----
#     ...
#     -----END OPENSSH PRIVATE KEY-----

# Placeholder - replace with actual secrets
example_secret: "replace-me-with-actual-secret"
